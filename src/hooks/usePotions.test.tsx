import {renderHook, act} from '@testing-library/react-hooks';
import UsePotion from './usePotions';

describe("UsePotions Custom Hook", () => {
    test("Should load potions from context.", () => {
        throw new Error("Load potions loaded in potion mixer");
      });
    test("Should load damage metrics by config.", () => {
      throw new Error("Load data");
    });
    test("Should calculate by damage bonus per potion.", () => {
        throw new Error("Calculate damage per potion");
    });
    test("Should run thorught iterative algorithm till get best usage option.", () => {
        throw new Error("Run algorithm");
    });
    test("Should use all the potions", () => {
        throw new Error("Run no potions left");
    });
  });
  